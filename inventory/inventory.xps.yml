interception:
  hosts:
    localhost:
      become: false
      connection: local
      ansible_user: "{{ lookup('env', 'USER')}}"
      LINK: /dev/input/by-path/platform-i8042-serio-0-event-kbd

xmodmap:
  hosts:
    localhost:
      become: false
      connection: local
      ansible_user: "{{ lookup('env', 'USER')}}"

better_file_dialog:
  hosts:
    localhost:
      become: false
      connection: local
      ansible_user: "{{ lookup('env', 'USER')}}"

# zsh:
#   hosts:
#     localhost:
#       become: false
#       connection: local
#       ansible_user: "{{ lookup('env', 'USER')}}"